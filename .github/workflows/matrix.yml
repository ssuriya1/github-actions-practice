name: Test Matrix

on: workflow_dispatch

jobs:
    Job1:
        runs-on: ubuntu-latest
        strategy:
            max-parallel: 1
            matrix:
                test1: ["one", "two", "three"]
                test2: ["four", "five", "six"]
        
        steps:
            - name: test
              env:
                  testEnv: something
              run: echo ${{env.testEnv}}${{matrix.test1}}
