name: 'New Custom Action'
description: 'Creating new custom action for practice'
runs: 
  using: 'composite'
  steps: 
    - name: custom-action
      run: echo Created new custom action
      shell: bash
